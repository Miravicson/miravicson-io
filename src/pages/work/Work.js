import React from "react";
import withLayout from "../../routes/withLayout";

function Work() {
  return (
    <section className="work">
      <h2>About my work as a software developer</h2>
    </section>
  );
}

export default withLayout(Work);
